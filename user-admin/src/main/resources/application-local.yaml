spring:
  config:
    activate:
      on-profile: local
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
  application:
    name: user-admin

server:
  port: 8083

application:
  topics:
    user: user